% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_results.R
\name{plot_results}
\alias{plot_results}
\title{Plot a volcano plot of differential expression results}
\usage{
plot_results(deg_results, padj_threshold = 0.05, log2fc_threshold = 1, ...)
}
\arguments{
\item{deg_results}{A data.frame as returned by \code{\link[=run_deg]{run_deg()}}, with
columns \code{log2fc} and \code{padj}.}

\item{padj_threshold}{Adjusted p-value significance threshold.
Default \code{0.05}.}

\item{log2fc_threshold}{Absolute log2 fold-change threshold. Default \code{1}.}

\item{...}{Additional arguments passed on to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly, a logical vector indicating which genes were
highlighted as significant.
}
\description{
Draws a basic volcano plot (log2 fold change vs. -log10 adjusted
p-value) from the output of \code{\link[=run_deg]{run_deg()}}, using base R graphics only.
Genes passing both the fold-change and significance thresholds are
highlighted.
}
\examples{
deg <- data.frame(
  gene = paste0("gene", 1:5),
  log2fc = c(-3, -0.2, 0.1, 2.5, 4),
  pvalue = c(0.001, 0.6, 0.9, 0.02, 0.0001),
  padj = c(0.005, 0.7, 0.95, 0.04, 0.0005)
)
plot_results(deg)

}
