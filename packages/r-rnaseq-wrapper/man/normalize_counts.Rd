% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_counts.R
\name{normalize_counts}
\alias{normalize_counts}
\title{Normalize a raw RNA-seq count matrix}
\usage{
normalize_counts(counts, method = c("cpm", "log2cpm"))
}
\arguments{
\item{counts}{A numeric matrix of raw counts, genes x samples (as
returned by \code{\link[=load_counts]{load_counts()}}).}

\item{method}{One of \code{"cpm"} (counts per million) or \code{"log2cpm"}
(\code{log2(cpm + 1)}).}
}
\value{
A numeric matrix of the same shape as \code{counts}, normalized.
}
\description{
Applies simple library-size normalization. This is intentionally basic
(counts-per-million, optionally log2-transformed) rather than a full
Bioconductor-style normalization (e.g. DESeq2's median-of-ratios or
edgeR's TMM) so that this package has no Bioconductor dependencies.
}
\examples{
counts <- matrix(c(10, 20, 0, 5, 100, 200, 10, 50), nrow = 2, byrow = TRUE)
normalize_counts(counts, method = "cpm")

}
