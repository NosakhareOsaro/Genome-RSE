% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_deg.R
\name{run_deg}
\alias{run_deg}
\title{Run a basic two-group differential expression analysis}
\usage{
run_deg(counts, group, pseudocount = 1)
}
\arguments{
\item{counts}{A numeric matrix, genes x samples.}

\item{group}{A vector (factor, character, or numeric) of length
\code{ncol(counts)} with exactly two distinct values, giving each sample's
group membership.}

\item{pseudocount}{A small constant added before log2 fold-change
calculation to avoid \code{log2(0)}. Default \code{1}.}
}
\value{
A data.frame with one row per gene (row names preserved as a
\code{gene} column) and columns \code{log2fc}, \code{pvalue}, and \code{padj}, sorted by
ascending \code{padj}.
}
\description{
For each gene (row), compares expression between two groups using a
Welch two-sample t-test and computes a log2 fold change of group means.
P-values are adjusted for multiple testing with the Benjamini-Hochberg
procedure.
}
\details{
This is a simplified workflow intended for teaching/demonstration: it
does not model count data with a negative binomial distribution the way
DESeq2 or edgeR do, and assumes \code{counts} has already been normalized
(e.g. with \code{\link[=normalize_counts]{normalize_counts()}}) if that matters for your use case.
}
\examples{
counts <- matrix(
  c(10, 12, 11, 100, 110, 105, 5, 6, 4, 5, 4, 6),
  nrow = 2, byrow = TRUE,
  dimnames = list(c("gene1", "gene2"), paste0("sample", 1:6))
)
group <- c("control", "control", "control", "treated", "treated", "treated")
run_deg(counts, group)

}
