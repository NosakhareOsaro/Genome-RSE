apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: model-serving
  labels:
    app.kubernetes.io/name: model-serving
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: model-serving
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
